package controls.text.syntax

/**
 * Enumeration of possible token types for syntax highlighting.
 */
enum class TokenType {
    KEYWORD, STRING, NUMBER, COMMENT, ANNOTATION, TYPE, METHOD, OPERATOR, PLAIN
}